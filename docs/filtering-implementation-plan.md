# План реализации фильтрации публикаций

## 1. Хранение правила

- [x] Добавить миграцию `migrations/0003_selection_rules.sql`.
- [x] Добавить в `tasks` nullable-поле `selection_rule TEXT`.
- [x] Хранить правило в каноническом виде команды, например `+clojure +lisp -реклама`.
- [x] Считать `NULL` отсутствием правила: такая задача пропускает все публикации.

## 2. Команда `/filter`

- [x] Добавить обработчик `src/commands/filter.clj`.
- [x] Поддержать `/filter <номер задачи> +слово -слово`.
- [x] Поддержать `/filter <номер задачи> off` для удаления правила.
- [x] Разрешить правила только с обязательными словами, только с запрещёнными словами или с обоими видами.
- [x] Разделять ключи пробелами и сохранять значимые символы внутри ключа, включая `C++`, `.NET` и `@account`.
- [x] Отклонять всю команду, если номер задачи некорректен, правило пустое или хотя бы один ключ не начинается с `+` либо `-`.
- [x] Обновлять только задачу отправителя, выбранную по её текущему номеру из `/tasks`.
- [x] При повторной команде полностью заменять прежнее правило.
- [x] Подключить обработчик в `src/main.clj`.
- [x] Добавить `/filter` в справку `src/commands/start.clj`.

## 3. Отображение правила

- [x] Выбирать `selection_rule` в `src/commands/tasks.clj`.
- [x] Показывать правило рядом с каналом, например `2. https://t.me/example (+clojure -реклама)`.
- [x] Не добавлять пометку для задач без правила.

## 4. Чтение текста публикаций

- [x] Расширить разбор Telegram Preview в `src/telegram.clj`, чтобы вместе с ID возвращались текст или подпись каждой публикации.
- [x] Сохранить текущую проверку доступности канала при `/add`, используя ID разобранных публикаций.
- [x] Считать публикацию без доступного текста анализируемой с неопределённым результатом и пропускать её.
- [x] Оставить разбор локальным для существующего HTML Telegram Preview, без нового API и зависимостей.

## 5. Проверка ключевых слов

- [x] Разбирать сохранённое правило на обязательные и запрещённые ключи.
- [x] Сравнивать без учёта регистра.
- [x] Экранировать значимые символы ключа, а границы проверять по Unicode-буквам и цифрам, чтобы `мат` не совпадал с `математика`, а `C++` совпадал с `C++.`.
- [x] Считать публикацию подходящей, если найден хотя бы один обязательный ключ либо обязательных ключей нет.
- [x] После этого отклонять публикацию при наличии любого запрещённого ключа.
- [x] Отправлять публикацию при отсутствии текста или невозможности уверенно выполнить проверку.

## 6. Планировщик

- [x] Добавить `selection_rule` в выборку задач в `src/main.clj`.
- [x] Обрабатывать новые публикации по возрастанию ID, как сейчас.
- [x] Отправлять уведомление только для подходящей публикации.
- [x] Для отфильтрованной публикации молча обновлять курсор без отправки сообщения.
- [x] Не пересматривать старые публикации после создания, замены или удаления правила.
- [x] Сохранить текущее поведение задач без правила и текущую изоляцию ошибок между задачами.

## 7. Проверки

- [x] Обновить тестовый HTML в `test/effect_test_entrypoint.clj`, добавив публикации с текстом, HTML-разметкой и без текста.
- [x] Проверить создание, полную замену и удаление правила через `/filter`.
- [x] Проверить отказ для неверного номера, отсутствующей задачи и некорректного ключа без изменения данных.
- [x] Проверить отображение правила в `/tasks`.
- [x] Проверить регистронезависимое совпадение целого обычного слова.
- [x] Проверить ключи с пунктуацией: `C++`, `.NET` и `@account`.
- [x] Проверить логику «любое обязательное», приоритет запрещённого ключа и односторонние правила.
- [x] Проверить отправку публикации без текста и обычное поведение задачи без правила.
- [x] Проверить отсутствие уведомления и продвижение курсора для отфильтрованной публикации.
- [x] Выполнить `make test`.

## Готовность

- [x] Владелец может настроить, заменить, посмотреть и удалить правило отдельной командой.
- [x] Планировщик применяет правило только к новым публикациям и не задерживается на скрытых публикациях.
- [x] Существующие задачи продолжают работать без настройки фильтра.
- [x] Все тесты проходят.
